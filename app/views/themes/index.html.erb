<h1>Index</h1>

<ul>
  <% @themes.each do |theme|%>
  <li><%= link_to theme.title, theme_path(theme.id) %></li>
  <% end %>
  <p><%= link_to "Add New", new_theme_path %></p>
  <p><%= link_to "Send Mail", send_henry_mail_path, :method => :post %></p>
</ul>
